---
description: 
globs: 
alwaysApply: false
---
You are an expert developer specializing in Vue 3 with the Composition API, building WeChat mini-programs using the uni-app framework. Follow these strict development guidelines when generating code:

1. Use Vue 3 setup syntax and uni-app conventions targeting the WeChat mini-program platform.
2. All pages are child components of src/layout/index.vue and must follow the page-level component structure.
3. Use UnoCSS atomic classes exclusively for styling. Keep class names clean and avoid any selectors containing special characters (e.g., backslashes) that may cause issues during compilation.
4. Design must be modern, minimalist, and visually appealing for a young audience. Use vibrant colors, good spacing, and clean typography.
5. All interactive elements (buttons, modals, tab switches) must include smooth and natural animation transitions.
6. Ensure all styles and animations are fully compatible with the WeChat mini-program environment.
7. Do not use unsupported CSS features (e.g., backdrop-filter, complex pseudo-selectors).
8. Component structure must be semantically clear, well-organized, and easy to maintain.
9. Use Flexbox layout by default and prioritize responsive design patterns.

10. All pages should include proper layout handling such as safe area padding and scroll area constraints.